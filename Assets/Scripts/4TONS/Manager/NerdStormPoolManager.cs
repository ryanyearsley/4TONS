using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NerdStormPoolManager : MonoBehaviour
{/*
	private Queue<PlayerObjectInstance> playerObjectPool;
	private Dictionary<int, Queue<ObjectInstance>> genericPoolDictionary = new Dictionary<int, Queue<ObjectInstance>> ();
	private Dictionary<int, Queue<ObjectInstance>> creatureDictionary = new Dictionary<int, Queue<ObjectInstance>> ();
	private Dictionary<int, Queue<ObjectInstance>> spellObjectDictionary = new Dictionary<int, Queue<ObjectInstance>> ();

	// Singleton Pattern to access this script with ease
	public static NerdStormPoolManager instance;
	private void Awake () {
		if (instance == null) {
			instance = this;
		} else if (instance != this) {
			Destroy (gameObject);
		}
	}
	public void CreatePlayerObjectPool (GameObject prefab, int poolSize) {

	}

	public void CreateCreatureObjectPool (GameObject prefab, int poolSize) {

	}
	public void CreateGenericPool(GameObject prefab, int poolSize) {

	}

	public void ReusePlayerObject(GameObject prefab, )

*/

}
